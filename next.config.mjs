/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['api.mineatar.io', 'cdn.prod.website-files.com', 'img.youtube.com'],
      },
    
};

export default nextConfig;
